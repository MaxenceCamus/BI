{% trans_default_domain 'FOSUserBundle' %}

<form action="{{ path("fos_user_security_check") }}" method="post">
    {% if csrf_token %}
        <input type="hidden" name="_csrf_token" value="{{ csrf_token }}" />
    {% endif %}
    <div class="form-group">
        <label for="username">Adresse Email</label>
        <input type="email" class="form-control" id="username" name="_username" value="{{ last_username }}" required="required" autocomplete="username" />
    </div>
    <div class="form-group">
        <label for="password">{{ 'security.login.password'|trans }}</label>
        <input type="password" class="form-control" id="password" name="_password" required="required" autocomplete="current-password" />
    </div>

    <div class="form-group">
        <div class="form-check form-check-flat">
            <label for="remember_me" class="form-check-label">
                <input type="checkbox" id="remember_me" name="_remember_me" value="on" class="form-check-input"/>
                {{ 'security.login.remember_me'|trans }}<i class="input-helper"></i>
            </label>
        </div>
    </div>
    {% if error %}
        <div class="text-center text-danger">{{ error.messageKey|trans(error.messageData, 'security') }}</div>
    {% endif %}
    <div class="row justify-content-between">
        <div class="col-auto">
            <input type="submit" id="_submit" name="_submit" class="btn btn-success mr-2" value="{{ 'security.login.submit'|trans }}" />
        </div>
        <div class="col-auto">
            <a href="{{ path('fos_user_resetting_request') }}">J'ai oublié mot mot de passe</a>
        </div>
    </div>
</form>
